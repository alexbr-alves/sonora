# Sonora Connect para macOS

O código do aplicativo para macOS fica neste workspace. O processo principal e a ponte segura com a interface estão em `/apps/macos/runtime`; o código compartilhado de interface permanece em `/src/desktop`.

O Electron é somente a tecnologia interna de empacotamento. O nome exibido ao usuário é **Sonora Connect**.
